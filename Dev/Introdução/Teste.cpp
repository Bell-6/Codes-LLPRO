#include < stdio.h > 
int main ( ){
    hoat x,y,z, media ;
print ("Digite as notas");
     scanf ("%f, %f, %f" &x, &y, &z);
media =  (x+y+z)/3;
    printf ("A media é %f",media );
return 0;

           }
